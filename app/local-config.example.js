'use strict';
// Local, PRIVATE configuration — copy this file to local-config.js and fill in.
// local-config.js is gitignored: your key never enters the repo.
//
//   cp app/local-config.example.js app/local-config.js
//
// The demo also accepts a key typed into its Voice settings panel
// (stored in your browser's localStorage); this file just saves the retyping.
window.CLEARREAD_CONFIG = {
  elevenLabsKey: ''  // paste your ElevenLabs API key between the quotes
};
